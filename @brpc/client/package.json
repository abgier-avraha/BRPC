{
  "name": "@brpc/client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build",
    "preview": "vite preview",
    "watch": "vitest",
    "test": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.4.9",
    "vitest": "^0.34.2"
  },
  "dependencies": {
    "cross-fetch": "^4.0.0"
  }
}
